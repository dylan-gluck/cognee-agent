# Test Monorepo

This is a test repository for validating TypeScript extraction support.

## Structure

- `python/` - Python source files
- `typescript/` - TypeScript/TSX source files

## Purpose

Used to validate that the cognee agent can process both Python and TypeScript codebases correctly.
